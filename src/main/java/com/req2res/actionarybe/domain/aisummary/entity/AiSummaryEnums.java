package com.req2res.actionarybe.domain.aisummary.entity;

public class AiSummaryEnums {

    public enum SourceType {
        FILE, URL
    }

    public enum Status {
        PENDING, RUNNING, SUCCEEDED, FAILED
    }
}
